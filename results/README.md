# Cross-build results

Same schema everywhere so Build 10 can compare cost/latency/quality honestly.

Per-build raw dumps live under `builds/NN-*/results/` (often gitignored).  
Summaries and comparison tables land here and in the root README.
